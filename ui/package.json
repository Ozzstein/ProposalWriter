{
  "name": "proposal-agency-ui",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "typecheck": "npm --workspace shared run typecheck && npm --workspace web run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

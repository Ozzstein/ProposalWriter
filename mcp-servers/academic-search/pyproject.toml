[project]
name = "academic-search-mcp"
version = "0.1.0"
description = "MCP server for PubMed academic paper search"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0.0",
    "httpx>=0.27.0",
]
